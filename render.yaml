services:
  - type: web
    name: droply-downloader
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: YOUTUBE_COOKIES_B64
        sync: false
